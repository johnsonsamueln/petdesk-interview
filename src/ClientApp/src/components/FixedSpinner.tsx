import * as React from "react"
export const FixedSpinner: React.FC = () => {
    return (
        <div className="fixed-spinner-bg">
            <div className="spinner-border fixed-spinner" />
        </div>
    )
}